services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: vozeen
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - vozeen_data:/var/lib/postgresql/data
volumes:
  vozeen_data:
